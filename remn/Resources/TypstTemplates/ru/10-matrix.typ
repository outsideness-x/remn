$ F_(mu nu) = mat(
  0, B_z, -B_y, -i E_x;
  -B_z, 0, B_x, -i E_y;
  B_y, -B_x, 0, -i E_z;
  i E_x, i E_y, i E_z, 0
) $

$ dot(x)_j = q/c sum_l dot(x)_l { (partial A_l)/(partial x_j) - (partial A_j)/(partial x_l) } - (partial A_j)/(partial t) - c (partial phi)/(partial x_j) $
